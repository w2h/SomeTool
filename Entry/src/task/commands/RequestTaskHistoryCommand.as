package task.commands
{
   import RSModel.command.RSModelCommand;
   
   public class RequestTaskHistoryCommand extends RSModelCommand
   {
       
      public function RequestTaskHistoryCommand()
      {
         super();
      }
   }
}
